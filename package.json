{
  "name": "beijing-infinite-loop",
  "version": "1.0.0",
  "description": "Beijing Infinite Loop — a sparse generative study of one ring, one axis and one seamless recursive breath.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "npm run gen:map && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "gen:map": "node scripts/generate-map.mjs",
    "verify": "npm run verify:geometry && npm run typecheck",
    "verify:geometry": "node scripts/geometry-check.mjs",
    "verify:browser": "node scripts/verify.mjs",
    "verify:seam": "node scripts/seam-check.mjs"
  },
  "dependencies": {
    "p5": "^1.11.10"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/p5": "^1.7.6",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
