{
  "name": "beijing-infinite-loop",
  "version": "1.0.0",
  "description": "Beijing Infinite Loop — an interactive generative-art tracer of Beijing's ring roads and central axis, with an infinitely zooming fractal loop.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "npm run gen:map && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "gen:map": "node scripts/generate-map.mjs"
  },
  "dependencies": {
    "p5": "^1.11.10"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/p5": "^1.7.6",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
